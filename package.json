{
  "name": "avukat-bilgi-sistemi",
  "private": true,
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "cd Panel && npm run dev",
    "build": "cd Panel && npm run build",
    "preview": "cd Panel && npm run preview",
    "preview:8000": "cd Panel && npm run preview:8000",
    "install": "cd Panel && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
